#!/bin/bash

# Claude OAuth-Only Wrapper
# Forces Claude Code to use stored OAuth session by removing API key interference

# Completely remove ANTHROPIC_API_KEY from environment to let Claude use stored OAuth
exec env -u ANTHROPIC_API_KEY claude "$@"