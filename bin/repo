#!/usr/bin/env bash
# Generated by Purpose Tool from .purpose/commands.json. Do not edit.
set -euo pipefail
ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd -P)"
resolve_jj_store() {
  local checkout="$1" marker ref
  marker="$checkout/.jj/repo"
  if [[ -d "$marker" && ! -L "$marker" ]]; then
    realpath -e "$marker" 2>/dev/null
    return
  fi
  if [[ -f "$marker" && ! -L "$marker" ]]; then
    IFS= read -r ref < "$marker" || [[ -n "$ref" ]] || return 1
    [[ -n "$ref" ]] || return 1
    if [[ "$ref" == /* ]]; then
      realpath -e "$ref" 2>/dev/null
    else
      realpath -e "$checkout/.jj/$ref" 2>/dev/null
    fi
    return
  fi
  return 1
}
find_caller_workspace() {
  local candidate
  candidate="$(pwd -P)"
  while [[ "$candidate" != "/" ]]; do
    if [[ -d "$candidate/.jj" && ! -L "$candidate/.jj" &&
          -f "$candidate/.purpose/commands.json" && ! -L "$candidate/.purpose/commands.json" &&
          -f "$candidate/bin/repo" && ! -L "$candidate/bin/repo" &&
          -x "$candidate/bin/repo" ]]; then
      printf '%s
' "$candidate"
      return 0
    fi
    candidate="$(dirname "$candidate")"
  done
  return 1
}
# A long-lived client may retain another checkout's generated executable in
# PATH. Rebind only to a regular generated launcher in a checkout that shares
# this launcher's jj store; task-write ownership is still enforced downstream.
if CALLER_ROOT="$(find_caller_workspace)" && [[ "$CALLER_ROOT" != "$ROOT" ]]; then
  ROOT_STORE="$(resolve_jj_store "$ROOT" || true)"
  CALLER_STORE="$(resolve_jj_store "$CALLER_ROOT" || true)"
  if [[ -n "$ROOT_STORE" && "$CALLER_STORE" == "$ROOT_STORE" ]]; then
    exec "$CALLER_ROOT/bin/repo" "$@"
  fi
fi
unset CALLER_ROOT ROOT_STORE CALLER_STORE
show_help() {
  cat <<'PURPOSE_REPO_HELP'
Usage: repo <command> [args...]

Commands are listed like MCP tools: what, when, steps, falsifier, and access mode.

Environment:
  Load the repository environment through direnv.

  Steps:
    1. Run `direnv allow`, then `eval "$(direnv export bash)"` once per session.
    2. Use bare `repo <command>` for the rest of that session.
    3. Use `direnv exec . repo <command>` only for a one-shot command when the shell cannot persist.
  Falsifier: If `repo` is unavailable or resolves outside this repository, stop and load direnv.

Checks:
  Prove environment, packages, and contract gates before mutating.

  repo check
    What: Run the dotfiles repository verification gate
    When: Before handoff or after changing the behavior this gate covers.
    Steps:
      1. Run `repo check`.
      2. Verify the declared result before continuing to the next lifecycle step.
    Falsifier: A non-zero exit or failed verification means this lifecycle step is incomplete.
  repo check nix
    What: Verify the exact dotfiles Nix environment without running package checks
    When: Before handoff or after changing the behavior this gate covers.
    Steps:
      1. Run `repo check nix`.
      2. Verify the declared result before continuing to the next lifecycle step.
    Falsifier: A non-zero exit or failed verification means this lifecycle step is incomplete.

Version control:
  Read state, isolate work, mutate, then land/publish.

  repo vcs status
    What: Show repository status
    When: Before choosing a repository lifecycle transition.
    Steps:
      1. Run `repo vcs status`.
      2. Read back repository state through `repo vcs` before another transition.
    Falsifier: A non-zero exit or failed verification means this lifecycle step is incomplete.
  repo vcs diff
    What: Show repository changes
    When: Before choosing a repository lifecycle transition.
    Steps:
      1. Run `repo vcs diff`.
      2. Read back repository state through `repo vcs` before another transition.
    Falsifier: A non-zero exit or failed verification means this lifecycle step is incomplete.
  repo vcs log
    What: Show revision history
    When: Before choosing a repository lifecycle transition.
    Steps:
      1. Run `repo vcs log`.
      2. Read back repository state through `repo vcs` before another transition.
    Falsifier: A non-zero exit or failed verification means this lifecycle step is incomplete.
  repo vcs show
    What: Show one revision
    When: Before choosing a repository lifecycle transition.
    Steps:
      1. Run `repo vcs show`.
      2. Read back repository state through `repo vcs` before another transition.
    Falsifier: A non-zero exit or failed verification means this lifecycle step is incomplete.
  repo vcs describe
    What: Commit the current workspace changes
    When: Before choosing a repository lifecycle transition.
    Steps:
      1. Run `repo vcs describe`.
      2. Read back repository state through `repo vcs` before another transition.
    Falsifier: A non-zero exit or failed verification means this lifecycle step is incomplete.
  repo vcs rebase
    What: Rebase the owned task branch onto one revision
    When: Before choosing a repository lifecycle transition.
    Steps:
      1. Run `repo vcs rebase`.
      2. Read back repository state through `repo vcs` before another transition.
    Falsifier: A non-zero exit or failed verification means this lifecycle step is incomplete.
  repo vcs fetch
    What: Fetch the Forgejo integration remote
    When: Before choosing a repository lifecycle transition.
    Steps:
      1. Run `repo vcs fetch`.
      2. Read back repository state through `repo vcs` before another transition.
    Falsifier: A non-zero exit or failed verification means this lifecycle step is incomplete.
  repo vcs land
    What: Verify and publish a clean task workspace as main
    When: Before choosing a repository lifecycle transition.
    Steps:
      1. Run `repo vcs land`.
      2. Read back repository state through `repo vcs` before another transition.
    Falsifier: A non-zero exit or failed verification means this lifecycle step is incomplete.
  repo vcs push
    What: Verify and publish local main to both remotes
    When: Before choosing a repository lifecycle transition.
    Steps:
      1. Run `repo vcs push`.
      2. Read back repository state through `repo vcs` before another transition.
    Falsifier: A non-zero exit or failed verification means this lifecycle step is incomplete.
  repo vcs amend
    What: Correct the message of an unpushed task-head commit without changing its contents
    When: Before choosing a repository lifecycle transition.
    Steps:
      1. Run `repo vcs amend`.
      2. Read back repository state through `repo vcs` before another transition.
    Falsifier: A non-zero exit or failed verification means this lifecycle step is incomplete.
  repo vcs branch-retire
    What: Retire an unused leftover namespaced ref; dry-run unless --apply
    When: Before choosing a repository lifecycle transition.
    Steps:
      1. Run `repo vcs branch-retire`.
      2. Read back repository state through `repo vcs` before another transition.
    Falsifier: A non-zero exit or failed verification means this lifecycle step is incomplete.
  repo vcs push-github
    What: Verify and publish local main to GitHub only
    When: Before choosing a repository lifecycle transition.
    Steps:
      1. Run `repo vcs push-github`.
      2. Read back repository state through `repo vcs` before another transition.
    Falsifier: A non-zero exit or failed verification means this lifecycle step is incomplete.
  repo vcs sync-main
    What: Converge a clean patch-equivalent canonical main to origin/main
    When: Before choosing a repository lifecycle transition.
    Steps:
      1. Run `repo vcs sync-main`.
      2. Read back repository state through `repo vcs` before another transition.
    Falsifier: A non-zero exit or failed verification means this lifecycle step is incomplete.
  repo vcs test
    What: Verify the repository VCS facade contract
    When: Before choosing a repository lifecycle transition.
    Steps:
      1. Run `repo vcs test`.
      2. Read back repository state through `repo vcs` before another transition.
    Falsifier: A non-zero exit or failed verification means this lifecycle step is incomplete.
  repo vcs worktree-abandon
    What: Discard an explicitly abandoned clean inactive task worktree
    When: Before choosing a repository lifecycle transition.
    Steps:
      1. Run `repo vcs worktree-abandon`.
      2. Read back repository state through `repo vcs` before another transition.
    Falsifier: A non-zero exit or failed verification means this lifecycle step is incomplete.
  repo vcs worktree-create
    What: Create an isolated task worktree
    When: Before choosing a repository lifecycle transition.
    Steps:
      1. Run `repo vcs worktree-create`.
      2. Read back repository state through `repo vcs` before another transition.
    Falsifier: A non-zero exit or failed verification means this lifecycle step is incomplete.
  repo vcs worktree-drop
    What: Remove an integrated clean task worktree
    When: Before choosing a repository lifecycle transition.
    Steps:
      1. Run `repo vcs worktree-drop`.
      2. Read back repository state through `repo vcs` before another transition.
    Falsifier: A non-zero exit or failed verification means this lifecycle step is incomplete.
  repo vcs worktree-list
    What: List registered worktrees
    When: Before choosing a repository lifecycle transition.
    Steps:
      1. Run `repo vcs worktree-list`.
      2. Read back repository state through `repo vcs` before another transition.
    Falsifier: A non-zero exit or failed verification means this lifecycle step is incomplete.

Changelog & sync:
  Record release fragments before synchronized version bumps.

  repo sync mise-upgrade
    What: Install and upgrade mise-managed tools
    When: When the command's named lifecycle action is the next verified step.
    Steps:
      1. Run `repo sync mise-upgrade`.
      2. Verify the declared result before continuing to the next lifecycle step.
    Falsifier: A non-zero exit or failed verification means this lifecycle step is incomplete.

Test:
  Run aggregate and focused repository test gates.

  repo test check-entry
    What: Verify bounded environment-check dispatch
    When: Before handoff or after changing the behavior this gate covers.
    Steps:
      1. Run `repo test check-entry`.
      2. Verify the declared result before continuing to the next lifecycle step.
    Falsifier: A non-zero exit or failed verification means this lifecycle step is incomplete.
  repo test codex-preferences
    What: Verify managed Codex preference preservation
    When: Before handoff or after changing the behavior this gate covers.
    Steps:
      1. Run `repo test codex-preferences`.
      2. Verify the declared result before continuing to the next lifecycle step.
    Falsifier: A non-zero exit or failed verification means this lifecycle step is incomplete.
PURPOSE_REPO_HELP
}
if [[ ${#} -eq 0 || "${1:-}" == "--help" || "${1:-}" == "help" ]]; then
  show_help
  exit 0
fi
if [[ "${1:-}" == 'check' ]] && [[ "${2:-}" == 'nix' ]]; then shift 2; if [[ "${1:-}" == "--help" || "${1:-}" == "-h" ]]; then show_help; exit 0; fi; exec "$ROOT/scripts/repo-check-nix.sh" "$@"
elif [[ "${1:-}" == 'sync' ]] && [[ "${2:-}" == 'mise-upgrade' ]]; then shift 2; if [[ "${1:-}" == "--help" || "${1:-}" == "-h" ]]; then show_help; exit 0; fi; exec "$ROOT/scripts/repo-maintenance.sh" 'mise-upgrade' "$@"
elif [[ "${1:-}" == 'test' ]] && [[ "${2:-}" == 'check-entry' ]]; then shift 2; if [[ "${1:-}" == "--help" || "${1:-}" == "-h" ]]; then show_help; exit 0; fi; exec "$ROOT/scripts/repo-check.sh" 'check-entry' "$@"
elif [[ "${1:-}" == 'test' ]] && [[ "${2:-}" == 'codex-preferences' ]]; then shift 2; if [[ "${1:-}" == "--help" || "${1:-}" == "-h" ]]; then show_help; exit 0; fi; exec "$ROOT/scripts/repo-check.sh" 'codex-preferences' "$@"
elif [[ "${1:-}" == 'vcs' ]] && [[ "${2:-}" == 'amend' ]]; then shift 2; if [[ "${1:-}" == "--help" || "${1:-}" == "-h" ]]; then show_help; exit 0; fi; exec "$ROOT/scripts/repo-vcs.sh" 'amend' "$@"
elif [[ "${1:-}" == 'vcs' ]] && [[ "${2:-}" == 'branch-retire' ]]; then shift 2; if [[ "${1:-}" == "--help" || "${1:-}" == "-h" ]]; then show_help; exit 0; fi; exec "$ROOT/scripts/repo-vcs.sh" 'branch-retire' "$@"
elif [[ "${1:-}" == 'vcs' ]] && [[ "${2:-}" == 'describe' ]]; then shift 2; if [[ "${1:-}" == "--help" || "${1:-}" == "-h" ]]; then show_help; exit 0; fi; exec "$ROOT/scripts/repo-vcs.sh" 'describe' "$@"
elif [[ "${1:-}" == 'vcs' ]] && [[ "${2:-}" == 'diff' ]]; then shift 2; if [[ "${1:-}" == "--help" || "${1:-}" == "-h" ]]; then show_help; exit 0; fi; exec "$ROOT/scripts/repo-vcs.sh" 'diff' "$@"
elif [[ "${1:-}" == 'vcs' ]] && [[ "${2:-}" == 'fetch' ]]; then shift 2; if [[ "${1:-}" == "--help" || "${1:-}" == "-h" ]]; then show_help; exit 0; fi; exec "$ROOT/scripts/repo-vcs.sh" 'fetch' "$@"
elif [[ "${1:-}" == 'vcs' ]] && [[ "${2:-}" == 'land' ]]; then shift 2; if [[ "${1:-}" == "--help" || "${1:-}" == "-h" ]]; then show_help; exit 0; fi; exec "$ROOT/scripts/repo-vcs.sh" 'land' "$@"
elif [[ "${1:-}" == 'vcs' ]] && [[ "${2:-}" == 'log' ]]; then shift 2; if [[ "${1:-}" == "--help" || "${1:-}" == "-h" ]]; then show_help; exit 0; fi; exec "$ROOT/scripts/repo-vcs.sh" 'log' "$@"
elif [[ "${1:-}" == 'vcs' ]] && [[ "${2:-}" == 'push' ]]; then shift 2; if [[ "${1:-}" == "--help" || "${1:-}" == "-h" ]]; then show_help; exit 0; fi; exec "$ROOT/scripts/repo-vcs.sh" 'push' "$@"
elif [[ "${1:-}" == 'vcs' ]] && [[ "${2:-}" == 'push-github' ]]; then shift 2; if [[ "${1:-}" == "--help" || "${1:-}" == "-h" ]]; then show_help; exit 0; fi; exec "$ROOT/scripts/repo-vcs.sh" 'push-github' "$@"
elif [[ "${1:-}" == 'vcs' ]] && [[ "${2:-}" == 'rebase' ]]; then shift 2; if [[ "${1:-}" == "--help" || "${1:-}" == "-h" ]]; then show_help; exit 0; fi; exec "$ROOT/scripts/repo-vcs.sh" 'rebase' "$@"
elif [[ "${1:-}" == 'vcs' ]] && [[ "${2:-}" == 'show' ]]; then shift 2; if [[ "${1:-}" == "--help" || "${1:-}" == "-h" ]]; then show_help; exit 0; fi; exec "$ROOT/scripts/repo-vcs.sh" 'show' "$@"
elif [[ "${1:-}" == 'vcs' ]] && [[ "${2:-}" == 'status' ]]; then shift 2; if [[ "${1:-}" == "--help" || "${1:-}" == "-h" ]]; then show_help; exit 0; fi; exec "$ROOT/scripts/repo-vcs.sh" 'status' "$@"
elif [[ "${1:-}" == 'vcs' ]] && [[ "${2:-}" == 'sync-main' ]]; then shift 2; if [[ "${1:-}" == "--help" || "${1:-}" == "-h" ]]; then show_help; exit 0; fi; exec "$ROOT/scripts/repo-vcs.sh" 'sync-main' "$@"
elif [[ "${1:-}" == 'vcs' ]] && [[ "${2:-}" == 'test' ]]; then shift 2; if [[ "${1:-}" == "--help" || "${1:-}" == "-h" ]]; then show_help; exit 0; fi; exec "$ROOT/scripts/repo-vcs.sh" 'contract-test' "$@"
elif [[ "${1:-}" == 'vcs' ]] && [[ "${2:-}" == 'worktree-abandon' ]]; then shift 2; if [[ "${1:-}" == "--help" || "${1:-}" == "-h" ]]; then show_help; exit 0; fi; exec "$ROOT/scripts/repo-vcs.sh" 'worktree-abandon' "$@"
elif [[ "${1:-}" == 'vcs' ]] && [[ "${2:-}" == 'worktree-create' ]]; then shift 2; if [[ "${1:-}" == "--help" || "${1:-}" == "-h" ]]; then show_help; exit 0; fi; exec "$ROOT/scripts/repo-vcs.sh" 'worktree-create' "$@"
elif [[ "${1:-}" == 'vcs' ]] && [[ "${2:-}" == 'worktree-drop' ]]; then shift 2; if [[ "${1:-}" == "--help" || "${1:-}" == "-h" ]]; then show_help; exit 0; fi; exec "$ROOT/scripts/repo-vcs.sh" 'worktree-drop' "$@"
elif [[ "${1:-}" == 'vcs' ]] && [[ "${2:-}" == 'worktree-list' ]]; then shift 2; if [[ "${1:-}" == "--help" || "${1:-}" == "-h" ]]; then show_help; exit 0; fi; exec "$ROOT/scripts/repo-vcs.sh" 'worktree-list' "$@"
elif [[ "${1:-}" == 'check' ]]; then shift 1; if [[ "${1:-}" == "--help" || "${1:-}" == "-h" ]]; then show_help; exit 0; fi; exec "$ROOT/scripts/repo-check.sh" "$@"
else
  echo "unknown command: $*" >&2
  show_help >&2
  exit 2
fi
