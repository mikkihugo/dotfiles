# Fresh, bounded reconnaissance only. Do not resume an OpenAI conversation.
# Reasoning effort stays unset until the deployed catalog exposes supported
# named effort values for this route.
model = "auto-fast"
model_provider = "llm-gateway"
sandbox_mode = "read-only"
web_search = "disabled"
