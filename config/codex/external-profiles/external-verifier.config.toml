# Fresh, bounded adversarial-verification task only. Do not resume an OpenAI
# conversation. This route is proven compatible with the Codex Responses
# reasoning contract; leave effort unset so Codex uses its supported default.
model = "auto-minimax"
model_provider = "llm-gateway"
sandbox_mode = "read-only"
web_search = "disabled"
