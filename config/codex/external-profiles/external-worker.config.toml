# Fresh, bounded implementation task only. Do not resume an OpenAI
# conversation. Reasoning effort stays unset until the deployed catalog exposes
# supported named effort values for this route.
model = "auto-glm"
model_provider = "llm-gateway"
sandbox_mode = "workspace-write"
web_search = "disabled"
