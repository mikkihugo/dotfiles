# Default provider stays OpenAI-compatible llm-gateway via ~/.local/bin/goose.
# Claude Code ACP: GOOSE_PROVIDER=claude-acp GOOSE_MODEL=current goose …
# ChatGPT/Codex ACP: GOOSE_PROVIDER=codex-acp GOOSE_MODEL=current goose …
#   uses ~/.codex OAuth (auth.json tokens); no OPENAI_API_KEY required.
# Requires: mise npm:@agentclientprotocol/{claude-agent-acp,codex-acp}.
GOOSE_PROVIDER: openai
GOOSE_MODEL: auto
claude-acp_configured: true
codex-acp_configured: true

extensions:
  centralcloud-mcp-gateway:
    name: centralcloud-mcp-gateway
    type: streamable_http
    url: http://mcp-gateway.svc/mcp
    enabled: true
    timeout: 300
