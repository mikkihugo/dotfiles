# Work: 2026-08-03-codex-profile-convergence

Purpose and executable requirements are in `purpose.contract.json` and `work.spec.json`.

The prior hosted-search packet is adjacent configuration history, not this task's contract: this packet owns only main-model convergence and the resident/external profile boundary.
