# Direnv dump strip

MCP `scaffold_work` cannot write `/home/mhugo/.dotfiles` (outside allowed roots).
This packet was created from the in-repo workspec/v4 shape used by
`docs/work/2026-08-14-host-resource-guardrails`.

Purpose: stop dump-cache hits from re-exporting `DIRENV_DIFF` / `DIRENV_WATCHES`.
