diff --git a/src/commands/start.rs b/src/commands/start.rs
index 8654317..9cfc266 100644
--- a/src/commands/start.rs
+++ b/src/commands/start.rs
@@ -49,12 +49,12 @@ pub fn run() {
         &envrc_dir.display().to_string(),
     );
 
-    // If not in a multiplexer, just run direnv synchronously
-    if Multiplexer::detect().is_none() {
+    // Fish cannot trap SIGUSR1, so keep its no-multiplexer fallback synchronous.
+    if Multiplexer::detect().is_none() && shell == Shell::Fish {
         run_direnv_sync(direnv, shell, true);
         return;
     }
 
     // Set up daemon context
     let ctx = match DaemonContext::new(parent_pid, envrc_dir, shell) {
         Ok(ctx) => ctx,
diff --git a/tests/ctrl_c_cancels_in_non_mux_mode.rs b/tests/ctrl_c_cancels_in_non_mux_mode.rs
index 1e4e199..61b4407 100644
--- a/tests/ctrl_c_cancels_in_non_mux_mode.rs
+++ b/tests/ctrl_c_cancels_in_non_mux_mode.rs
@@ -1,63 +1,50 @@
-//! Regression test for #7: SIGINT must cancel the synchronous direnv run,
-//! not restart it.
+//! Plain terminals must use the same non-blocking daemon path as multiplexers.
 
 mod common;
 use common::*;
-use nix::sys::signal::{Signal, kill};
-use nix::unistd::Pid;
-use std::io::Read;
+use std::fs;
 use std::time::{Duration, Instant};
 
 #[test]
-fn ctrl_c_cancels_direnv_in_non_mux_mode() {
+fn start_is_async_in_non_mux_mode() {
     let sb = Sandbox::new(
-        r#"for i in $(seq 1 10); do
-    sleep 1
-    echo "Processing step $i..." >&2
-done
+        r#"sleep 2
 export SLOW_TEST=completed
 "#,
     )
     .unwrap();
 
-    // No multiplexer vars => synchronous mode.
-    let env = sb.base_env();
-    let mut child = sb.spawn(&["start"], &env).unwrap();
-
-    // Confirm direnv started before we interrupt it.
-    let early = read_stderr_until(&mut child, "Processing step", Duration::from_secs(5));
-    assert!(
-        early.contains("Processing step"),
-        "direnv never started: {early}"
-    );
-
-    kill(Pid::from_raw(child.id() as i32), Signal::SIGINT).unwrap();
-
+    let sink = SignalSink::new().unwrap();
+    let mut env = sb.base_env();
+    env.insert(
+        "DIRENV_INSTANT_SHELL_PID".into(),
+        sink.pid().to_string().into(),
+    );
     let t0 = Instant::now();
-    let deadline = t0 + Duration::from_secs(8);
-    let status = loop {
-        if let Some(s) = child.try_wait().unwrap() {
-            break s;
-        }
-        if Instant::now() >= deadline {
-            child.kill().ok();
-            child.wait().ok();
-            panic!("did not terminate within 8s after SIGINT");
-        }
-        std::thread::sleep(Duration::from_millis(50));
-    };
-
-    let mut stdout = String::new();
-    child
-        .stdout
-        .take()
-        .unwrap()
-        .read_to_string(&mut stdout)
-        .ok();
-
-    assert!(!status.success(), "expected non-zero exit after SIGINT");
+    let output = sb.run(&["start"], &env).unwrap();
+    assert!(output.status.success());
     assert!(
-        !stdout.contains("SLOW_TEST"),
-        "operation completed despite SIGINT: {stdout}"
+        t0.elapsed() < Duration::from_secs(1),
+        "plain-terminal start blocked for {:?}",
+        t0.elapsed()
     );
+
+    let cache_root = sb.home.join(".cache/direnv-instant");
+    let deadline = Instant::now() + Duration::from_secs(8);
+    loop {
+        let ready = fs::read_dir(&cache_root)
+            .ok()
+            .into_iter()
+            .flatten()
+            .filter_map(Result::ok)
+            .any(|entry| entry.path().join("env").is_file());
+        if ready {
+            break;
+        }
+        assert!(Instant::now() < deadline, "daemon never published its environment");
+        std::thread::sleep(Duration::from_millis(50));
+    }
+
+    let stopped = sb.run(&["stop"], &env).unwrap();
+    assert!(stopped.status.success());
 }
diff --git a/tests/no_tmux_runs_synchronously.rs b/tests/no_tmux_runs_synchronously.rs
deleted file mode 100644
index 9a16e03..0000000
--- a/tests/no_tmux_runs_synchronously.rs
+++ /dev/null
@@ -1,38 +0,0 @@
-//! Without a multiplexer, `start` blocks and runs direnv synchronously.
-
-mod common;
-use common::*;
-use std::time::Instant;
-
-#[test]
-fn no_tmux_runs_direnv_synchronously() {
-    let sb = Sandbox::new("sleep 1\nexport SYNC_TEST=success\n").unwrap();
-    let env = sb.base_env();
-
-    let t0 = Instant::now();
-    let out = sb.run(&["start"], &env).unwrap();
-    let elapsed = t0.elapsed();
-
-    assert!(
-        elapsed.as_secs_f64() >= 0.9,
-        "returned too quickly: {elapsed:?}"
-    );
-    assert!(
-        out.status.success(),
-        "{}",
-        String::from_utf8_lossy(&out.stderr)
-    );
-
-    let stdout = String::from_utf8_lossy(&out.stdout);
-    let stderr = String::from_utf8_lossy(&out.stderr);
-    assert!(
-        stdout.contains("SYNC_TEST") || stderr.contains("SYNC_TEST"),
-        "missing export in stdout={stdout:?} stderr={stderr:?}"
-    );
-
-    // Synchronous mode does not start a daemon.
-    assert!(!stdout.contains("__DIRENV_INSTANT_ENV_FILE"));
-    assert!(!stdout.contains("__DIRENV_INSTANT_STDERR_FILE"));
-    // Always set so `stop` knows the working dir.
-    assert!(stdout.contains("__DIRENV_INSTANT_CURRENT_DIR"));
-}
