import { Command as CommanderCommand } from 'commander';
import { type ComponentType } from 'react';
import type { Command } from './internal-types.js';
import { type AppProps } from './types.js';
declare const generateCommands: (parentCommanderCommand: CommanderCommand, pastelCommands: Map<string, Command>, { appComponent }: {
    appComponent: ComponentType<AppProps>;
}) => void;
export default generateCommands;
